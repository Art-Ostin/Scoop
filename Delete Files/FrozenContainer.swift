//
//  FrozenHomeView.swift
//  Scoop
//
//  Created by Art Ostin on 24/01/2026.
//

/*
 
 import SwiftUI

 
 struct FrozenContainer: View {
     
     @Environment(\.appDependencies) private var dep
     
     @State var showSettings: Bool = false
     
     @State private var tabSelection: TabBarItem = .meet
     
     @State private var frozenSelection: Int = 0
     
     var body: some View {
         
         FrozenView(vm: FrozenViewModel(sessionManager: dep.sessionManager, cacheManager: dep.cacheManager), tabSelection:$frozenSelection)
             .overlay(alignment: .topLeading) {
                 SettingsButton(showSettingsView: $showSettings)
                     .padding(.horizontal)
         }
     }
 }

 extension FrozenContainer {
     private var frozenView: some View {
         FrozenView(vm: FrozenViewModel(sessionManager: dep.sessionManager, cacheManager: dep.cacheManager), tabSelection:$frozenSelection)
     }
 }
 */
