//
//  RespondMessageRow.swift
//  Scoop
//
//  Created by Art Ostin on 30/03/2026.
//

/*
 
 import SwiftUI

 struct RespondMessageSection: View {
     
     @Binding var showMessageScreen: Bool
     @Binding var showTimePopup: Bool
     let message: String
     
     var body: some View {
         VStack(spacing: 2) {
             HStack(spacing: 24) {
                 Image("SmallMessageIcon")
                     .scaleEffect(1.3)
                 
                 Group {
                     Text("You - ")
                         .foregroundStyle(Color.accent)
                     +
                     Text(message)
                 }
                 .respondTextFormat(showTimePopup: $showTimePopup.wrappedValue)
             }
             .frame(maxWidth: .infinity, alignment: .leading)
             
             OpenMessageButton(isEdit: false, showMessageView: $showMessageScreen)
                 .frame(maxWidth: .infinity, alignment: .trailing)
         }
     }
 }
 */
